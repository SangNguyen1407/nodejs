# Installation

## Node v6.x and later
> Latest version of phantom does `require Node v6.x and later`. You can install with

```bash
$ npm install express
```

```bash
$ npm install tedious
```
